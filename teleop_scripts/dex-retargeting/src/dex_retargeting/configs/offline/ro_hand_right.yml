retargeting:
  type: vector
  urdf_path: ro_hand/ro_hand_right.urdf

  # Target refers to the retargeting target, which is the robot hand
  target_joint_names: [  'lf_proximal_link', 'if_proximal_link', 'mf_proximal_link', 'rf_proximal_link', 'th_proximal_link' , 'th_root_link' ]
  target_origin_link_names: [ "base_link", "base_link", "base_link", "base_link", "base_link" ]
  target_task_link_names: [ "lf_tip_link", "if_tip_link", "mf_tip_link", "rf_tip_link", "th_tip_link" ]
  scaling_factor: 1.15

  # Source refers to the retargeting input, which usually corresponds to the human hand
  # The joint indices of human hand joint which corresponds to each link in the target_link_names
  target_link_human_indices: [ [ 0, 0, 0, 0, 0 ], [ 20, 8, 12, 16, 4 ] ]

  # A smaller alpha means stronger filtering, i.e. more smooth but also larger latency
  low_pass_alpha: 0.2
